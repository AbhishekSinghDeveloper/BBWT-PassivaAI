import { Component } from "@angular/core";

@Component({
    selector: "headings",
    templateUrl: "./headings.component.html"
})
export class HeadingsComponent {
    code = ".cl { color: 'red' }";
}