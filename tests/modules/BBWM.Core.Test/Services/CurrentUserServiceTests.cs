namespace BBWM.Core.Test.Services;

public class CurrentUserServiceTests
{
    public CurrentUserServiceTests()
    {
    }

    /*  private CurrentUserService<User> GetService()
      {
          var mockUserManager = new UserManager<User>();
          var mockHttpContextAccessor = new Mock<IHttpContextAccessor>();

          return new CurrentUserService<User>(
              mockUserManager.Object,
              mockHttpContextAccessor.Object);
      }

      [Fact]
      public void Get_Current_UserId_Test()
      {
          var service = GetService();
          service.GetCurrentUserId();

      }

      [Fact]
      public async Task Get_Current_User_Email_Test()
      {
          var service = GetService();
          service.GetCurrentUserEmail();
      }

      [Fact]
      public async Task Get_Current_User_Roles_Test()
      {
          var service = GetService();
          service.GetCurrentUserRoles();
      }*/
}
